Algoritmo "Fibonacci"

Var
ant, atual, prox: inteiro


Inicio
ant <- 0
atual <- 1
enquanto (prox <= 300) faça
escreva(prox)
prox <- ant + atual
ant <- atual
atual <- prox
fimenquanto

Fimalgoritmo